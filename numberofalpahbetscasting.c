/*@Shyed Shahriar Housaini
Copyright: @uthor*/

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>
#include <windows.h>
///#include <threads.h>
#include <conio.h>
/// #include <dos.h>
#include <direct.h>
int main(void)
{

    printf(
        R"( These things can be converted from char to integer and from integer to char
)");  /// R"() is for raw string conversion.

printf(
      R"( These things can be converted from char to integer and from integer to char
)");  /// R"() is for raw string conversion.

    char C1 = 'a';
    char C2 = 'b';
    char C3 = 'c';
    char C4 = 'd';
    char C5 = 'e';
    char C6 = 'f';
    char C7 = 'g';
    char C8 = 'h';
    char C9 = 'i';
    char C10 = 'j';
    char C11 = 'k';
    char C12 = 'l';
    char C13 = 'm';
    char C14 = 'n';
    char C15 = 'o';
    char C16 = 'p';
    char C17 = 'q';
    char C18 = 'r';
    char C19 = 's';
    char C20 = 't';
    char C21 = 'u';
    char C22 = 'v';
    char C23 = 'w';
    char C24 = 'x';
    char C25 = 'y';
    char C26 = 'z';

    printf(" In ASCII %i = %c ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c \n,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c \n ,In ASCII %i =%c,In ASCII %i =%c \n", (int)C1, C1, (int)C2, C2, C3, C3, C4, C4, C5, C5, C6, C6, C7, C7, C8, C8, (int)C9, C9, (int)C10, C10); /// (int) is optional. representing chars as integers. /// This is called casting.

    printf(" In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c\n ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c\n", C11, C11, C12, C12, C13, C13, C14, C14, C15, C15, C16, C16, (int)C17, C17, C18, C18, (int)C19, C19, (int)C20, C20); /// (int) is optional.representing chars as integers. /// This is called casting.

    printf(" In ASCII %i = %c ,In ASCII %i =%c ,In ASCII %i =%c\n ,In ASCII %i =%c ,In ASCII %i =%c ,In ASCII %i =%c \n", (int)C21, C21, C22, C22, (int)C23, C23, C24, C24, (int)C25, C25, (int)C26, C26); /// (int) is optional. representing chars as integers. /// This is called casting.

    return 0;
}
