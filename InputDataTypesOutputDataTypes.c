/*@Shyed Shahriar Housaini
Copyright: @uthor*/

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>
#include <windows.h>
///#include <threads.h>
#include <conio.h>
/// #include <dos.h>
#include <direct.h>

int main(void)

{
    int score1, score2, score3;

    score1 = 34;

    score2 = 43;

    score3 = 71;

    printf(" Input is int type data 34,43,71, calculate output data type with "
           "average output\n");
    printf(" i=%i\n", (score1 + score2 + score3) / 3);
    printf(" d=%d\n", (score1 + score2 + score3) / 3);
    printf(" f=%f\n", (score1 + score2 + score3) / 3);
    printf("lf=%lf\n", (score1 + score2 + score3) / 3);

    printf("HI\n");

    float score11, score21, score31;

    score11 = 34;

    score21 = 43;

    score31 = 71;

    printf(" Input is float type data 34,43,71, calculate output data type "
           "with average output\n");
    printf("i=%i\n", (score11 + score21 + score31) / 3);
    printf("d=%d\n", (score11 + score21 + score31) / 3);
    printf("f=%f\n", (score11 + score21 + score31) / 3);
    printf("lf=%lf\n", (score11 + score21 + score31) / 3);

    printf("HI\n");

    double score111, score121, score131;

    score111 = 34;

    score121 = 43;

    score131 = 71;

    printf(" Input is double type data 34,43,71, calculate output data type "
           "with average output\n");
    printf("i=%i\n", (score111 + score121 + score131) / 3);
    printf("d=%d\n", (score111 + score121 + score131) / 3);
    printf("f=%f\n", (score111 + score121 + score131) / 3);
    printf("lf=%lf\n", (score111 + score121 + score131) / 3);

    printf("HI\n");

    long score1111, score1121, score1131;

    score1111 = 34;

    score1121 = 43;

    score1131 = 71;

    printf(" Input is long type data 34,43,71, calculate output data type with "
           "average output\n");
    printf("i=%i\n", (score1111 + score1121 + score1131) / 3);
    printf("d=%d\n", (score1111 + score1121 + score1131) / 3);
    printf("f=%f\n", (score1111 + score1121 + score1131) / 3);
    printf("lf=%lf\n", (score1111 + score1121 + score1131) / 3);

    printf("HI\n");

    long long score11111, score11121, score11131;

    score11111 = 34;

    score11121 = 43;

    score11131 = 71;

    printf(" Input is long long type data 34,43,71, calculate output data type with average output\n");
    printf("i=%i\n", (score11111 + score11121 + score11131) / 3);
    printf("d=%d\n", (score11111 + score11121 + score11131) / 3);
    printf("f=%f\n", (score11111 + score11121 + score11131) / 3);
    printf("lf=%lf\n", (score11111 + score11121 + score11131) / 3);

    printf("HI\n");
    return 0;
}